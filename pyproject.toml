[project]
name = "ml4g-activation-steering"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "scikit-learn>=1.9.0",
    "transformers>=5.10.2",
]

[project.optional-dependencies]
gpu = [
    "accelerate>=1.13.0 ; sys_platform == 'linux'",
    "torch>=2.12.0 ; sys_platform == 'linux'",
]
